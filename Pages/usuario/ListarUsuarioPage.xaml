<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiFirebase.Pages.usuario.ListarUsuarioPage"
             xmlns:model="clr-namespace:MauiFirebase.Models"
             xmlns:viewModel="clr-namespace:MauiFirebase.PageModels.Usuarios"
             x:DataType="viewModel:UsuarioPageModel"
             Title="Usuarios Registrados">

    <CollectionView x:Name="UsuariosCollectionView" ItemsSource="{Binding Usuarios}">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="model:Usuario">
                <Frame BorderColor="Gray" CornerRadius="10" Margin="10">
                    <VerticalStackLayout Padding="10">
                        <Label Text="{Binding Nombre}" FontAttributes="Bold" FontSize="18"/>
                        <Label Text="{Binding Correo}" FontSize="14"/>
                        <Label Text="{Binding Rol}" FontSize="14" TextColor="Gray"/>
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>
